interface:
  display_name: "GitHub PR Operator"
  short_description: "Operate PRs with Japanese comments and clear blocked states."
  default_prompt: "Use available GitHub tooling for repository PR operations. Create or update PRs, acceptance comments, and self-review comments in Japanese when the workflow calls for them, suppress routine confirmations for reversible operations, and report blocked GitHub steps without claiming them complete."
